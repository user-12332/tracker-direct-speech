# Quick Start Guide 🚀

## Быстрый старт (5 минут)

### 1. Установка

```bash
# Установите зависимости
pip install -r requirements.txt

# Или по отдельности
pip install streamlit pandas openpyxl filelock
```

### 2. Запуск приложения

```bash
cd officials_tracker
streamlit run app.py
```

Откроется браузер на `http://localhost:8501`

### 3. Первые шаги

Данные уже импортированы из вашего Excel файла! 

В приложении вы увидите:
- **161 позицию**
- **628 персон**
- **1 тестовое упоминание** (для Грефа)

### 4. Попробуйте добавить упоминание

1. Откройте приложение
2. В боковом меню выберите **"➕ Добавить упоминание"**
3. Выберите любую персону (например: Набиуллина Эльвира Сахипзадовна)
4. Заполните форму:
   - **Дата**: сегодняшняя
   - **Источник**: РБК
   - **URL**: https://www.rbc.ru/example
   - **Заголовок**: Пример заголовка
   - **Текст**: Вставьте любой текст из новости
   - **Теги**: экономика, финансы
5. Нажмите **"Сохранить"**

✅ Упоминание сохранено!

### 5. Посмотрите результат

1. Перейдите в **"📰 Все упоминания"**
2. Вы увидите ваше упоминание в списке

### 6. Что дальше?

Теперь вы можете:

- ✅ Добавлять новые упоминания каждый день
- ✅ Следить за позициями чиновников
- ✅ Искать упоминания по персонам и источникам

## Для работы в команде через Google Drive

### Настройка за 3 шага:

1. **Создайте папку в Google Drive**
   - Назовите её `officials_tracker`
   - Расшарьте команде с правами редактирования

2. **Скопируйте проект в Google Drive**
   ```bash
   cp -r officials_tracker ~/Google\ Drive/
   ```

3. **Измените config.py**
   ```python
   # Было:
   BASE_PATH = Path(__file__).parent.resolve()
   
   # Стало:
   BASE_PATH = Path.home() / 'Google Drive' / 'officials_tracker'
   ```

4. **Запустите на каждом компьютере**
   ```bash
   cd ~/Google\ Drive/officials_tracker
   streamlit run app.py
   ```

Готово! Теперь все изменения синхронизируются автоматически.

## Полезные команды

### Добавить тестовое упоминание
```bash
python scripts/add_test_mention.py
```

### Импортировать новые данные из Excel
```bash
python scripts/import_from_excel.py
```

### Посмотреть статистику
```python
from src.core.storage import StorageManager
import config

storage = StorageManager(config.BASE_PATH)
print(storage.get_stats())
```

### Сделать бэкап
```bash
# Создайте копию папки data
cp -r data/ backup_$(date +%Y%m%d)/
```

## Troubleshooting

### Ошибка импорта модулей
```bash
# Убедитесь, что вы в правильной директории
cd officials_tracker
pwd
```

### Приложение не открывается
```bash
# Проверьте, что порт 8501 свободен
streamlit run app.py --server.port 8502
```

### Google Drive не синхронизируется
1. Проверьте, что Google Drive Desktop запущен
2. Убедитесь, что папка не "только для чтения"
3. Проверьте права доступа к папке

## Следующие шаги

1. 📖 Прочитайте полный [README.md](README.md)
2. 🎯 Добавьте реальные упоминания из новостей
3. 👥 Пригласите команду через Google Drive
4. 🤖 (Опционально) Настройте автоматический парсинг

Удачи! 🎉
