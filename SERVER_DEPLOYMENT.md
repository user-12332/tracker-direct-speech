# 🖥️ Серверное Хранение - Полное Руководство

**Перевод с файлового хранения на серверное**

---

## 🎯 Преимущества серверного хранения

### Сейчас (файловое):
- ❌ Нельзя работать одновременно
- ❌ Зависимость от Google Drive
- ❌ Медленная синхронизация
- ❌ Конфликты файлов

### С сервером:
- ✅ Несколько пользователей одновременно
- ✅ Быстрый доступ
- ✅ Центральная база данных
- ✅ Нет конфликтов
- ✅ Автоматические бэкапы
- ✅ Доступ из любой точки мира

---

## 📊 Варианты Реализации

### Вариант 1: Streamlit Cloud (САМЫЙ ПРОСТОЙ) ⭐

**Что это:**
- Бесплатный хостинг от Streamlit
- Никаких серверов не нужно
- Деплой за 5 минут

**Плюсы:**
- ✅ Бесплатно (для 1 приложения)
- ✅ Очень просто настроить
- ✅ Автоматические обновления
- ✅ HTTPS из коробки
- ✅ Всегда доступно

**Минусы:**
- ⚠️ Данные в публичном облаке
- ⚠️ Ограничения бесплатного плана (1GB RAM)

**Для кого:**
- Малые команды (2-5 человек)
- Не секретные данные
- Хотите быстро начать

**Цена:** $0 (бесплатно) или $20/месяц (больше ресурсов)

---

### Вариант 2: Ваш собственный сервер (Linux VPS) 💪

**Что это:**
- Арендуете виртуальный сервер
- Ставите PostgreSQL базу
- Разворачиваете приложение

**Плюсы:**
- ✅ Полный контроль
- ✅ Ваши данные на вашем сервере
- ✅ Можно масштабировать
- ✅ Неограниченные пользователи

**Минусы:**
- ⚠️ Нужно настраивать самому
- ⚠️ Нужно обслуживать
- ⚠️ Немного технических знаний

**Для кого:**
- Средние/большие команды
- Конфиденциальные данные
- Есть бюджет на сервер

**Цена:** $5-20/месяц (DigitalOcean, Hetzner, AWS Lightsail)

---

### Вариант 3: Корпоративный сервер (On-Premise) 🏢

**Что это:**
- Используете свой корпоративный сервер
- База данных внутри сети компании
- Максимальная безопасность

**Плюсы:**
- ✅ Максимальная безопасность
- ✅ Данные не покидают компанию
- ✅ Интеграция с AD/LDAP
- ✅ Полный контроль

**Минусы:**
- ⚠️ Нужен IT отдел
- ⚠️ Сложная настройка
- ⚠️ Высокие начальные затраты

**Для кого:**
- Крупные компании
- Строгие требования безопасности
- Есть IT отдел

**Цена:** Зависит от инфраструктуры

---

## 🚀 Вариант 1: Streamlit Cloud (Пошаговая Инструкция)

### Что нужно:
- ✅ GitHub аккаунт
- ✅ 10 минут времени

### Шаг 1: Подготовка проекта

**1.1. Создайте GitHub репозиторий:**
1. Зайдите на https://github.com
2. Нажмите "New repository"
3. Название: `officials-tracker`
4. Private (если данные конфиденциальны)
5. Создать

**1.2. Загрузите проект в GitHub:**

**Windows:**
```powershell
cd C:\Users\oskol\Documents\GitHub\officials_tracker

# Инициализация git (если ещё не сделано)
git init
git add .
git commit -m "Initial commit"

# Подключение к GitHub
git remote add origin https://github.com/ВАШ_ЮЗЕРНЕЙМ/officials-tracker.git
git push -u origin main
```

**Mac/Linux:**
```bash
cd ~/Documents/officials_tracker

git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ВАШ_ЮЗЕРНЕЙМ/officials-tracker.git
git push -u origin main
```

### Шаг 2: Деплой на Streamlit Cloud

**2.1. Зайдите на Streamlit Cloud:**
- https://share.streamlit.io/
- Sign in with GitHub

**2.2. Deploy новое приложение:**
1. Нажмите "New app"
2. Repository: выберите `officials-tracker`
3. Branch: `main`
4. Main file path: `app.py`
5. Нажмите "Deploy!"

**2.3. Подождите 2-5 минут**

Готово! Приложение доступно по URL:
```
https://ВАШ_ЮЗЕРНЕЙМ-officials-tracker.streamlit.app
```

### Шаг 3: Настройка доступа

**3.1. Отправьте ссылку коллегам:**
```
https://ВАШ_ЮЗЕРНЕЙМ-officials-tracker.streamlit.app
```

**3.2. Они просто открывают в браузере!**
- ✅ Не нужно устанавливать Python
- ✅ Не нужно скачивать проект
- ✅ Просто открыть ссылку

### Шаг 4: Обновление данных

**Когда нужно обновить (добавить новые позиции из CSV):**

```powershell
# На вашем компьютере
cd officials_tracker
python scripts/import_from_csv.py list_officials.csv

# Загрузить изменения на GitHub
git add data/
git commit -m "Updated data"
git push

# Streamlit Cloud автоматически обновится!
```

---

## 🖥️ Вариант 2: Свой VPS сервер (Пошаговая Инструкция)

### Что нужно:
- ✅ VPS сервер (Ubuntu 22.04)
- ✅ Доменное имя (опционально, но лучше)
- ✅ 30-60 минут на настройку

### Провайдеры VPS:

**DigitalOcean** (рекомендую) ⭐
- $6/месяц (Basic Droplet)
- https://digitalocean.com
- Простая панель управления

**Hetzner** (дешевле)
- €4/месяц
- https://hetzner.com
- Европейские сервера

**AWS Lightsail**
- $5/месяц
- https://lightsail.aws.amazon.com
- Интеграция с AWS

### Шаг 1: Создание сервера

**1.1. Арендуйте VPS:**
- ОС: Ubuntu 22.04 LTS
- RAM: минимум 1GB (лучше 2GB)
- Диск: 25GB SSD
- Регион: ближайший к вам

**1.2. Получите IP адрес:**
```
Пример: 123.45.67.89
```

**1.3. Подключитесь по SSH:**

**Windows (PowerShell):**
```powershell
ssh root@123.45.67.89
```

**Mac/Linux:**
```bash
ssh root@123.45.67.89
```

### Шаг 2: Установка на сервере

**2.1. Обновление системы:**
```bash
apt update && apt upgrade -y
```

**2.2. Установка Python и зависимостей:**
```bash
apt install -y python3 python3-pip python3-venv nginx postgresql
```

**2.3. Создание пользователя приложения:**
```bash
useradd -m -s /bin/bash officials
su - officials
```

**2.4. Загрузка проекта:**
```bash
# Если используете Git
git clone https://github.com/ВАШ_ЮЗЕРНЕЙМ/officials-tracker.git
cd officials-tracker

# Или скопируйте файлы через SCP
```

**2.5. Установка зависимостей:**
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

**2.6. Настройка PostgreSQL:**
```bash
# Вернитесь к root
exit

# Создайте базу данных
sudo -u postgres psql
```

В PostgreSQL консоли:
```sql
CREATE DATABASE officials_tracker;
CREATE USER officials_user WITH PASSWORD 'ваш_надёжный_пароль';
GRANT ALL PRIVILEGES ON DATABASE officials_tracker TO officials_user;
\q
```

### Шаг 3: Настройка автозапуска

**3.1. Создайте systemd сервис:**
```bash
nano /etc/systemd/system/officials-tracker.service
```

**Содержимое:**
```ini
[Unit]
Description=Officials Tracker
After=network.target

[Service]
Type=simple
User=officials
WorkingDirectory=/home/officials/officials-tracker
ExecStart=/home/officials/officials-tracker/venv/bin/streamlit run app.py --server.port 8501 --server.address 0.0.0.0
Restart=always

[Install]
WantedBy=multi-user.target
```

**3.2. Запустите сервис:**
```bash
systemctl daemon-reload
systemctl enable officials-tracker
systemctl start officials-tracker
systemctl status officials-tracker
```

### Шаг 4: Настройка Nginx (обратный прокси)

**4.1. Создайте конфиг Nginx:**
```bash
nano /etc/nginx/sites-available/officials-tracker
```

**Содержимое:**
```nginx
server {
    listen 80;
    server_name your-domain.com;  # Или IP адрес

    location / {
        proxy_pass http://localhost:8501;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

**4.2. Активируйте конфиг:**
```bash
ln -s /etc/nginx/sites-available/officials-tracker /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx
```

### Шаг 5: SSL сертификат (HTTPS)

**5.1. Установка Certbot:**
```bash
apt install -y certbot python3-certbot-nginx
```

**5.2. Получение сертификата:**
```bash
certbot --nginx -d your-domain.com
```

**Готово!** Теперь доступно по https://your-domain.com

### Шаг 6: Доступ для коллег

**Отправьте им ссылку:**
```
https://your-domain.com
```

Или если без домена:
```
http://123.45.67.89
```

**Они просто открывают в браузере!**
- ✅ Не нужно устанавливать ничего
- ✅ Просто URL
- ✅ Работают одновременно

---

## 🔄 Миграция данных на сервер

### Вариант A: Перенос существующих JSON файлов

**На вашем компьютере:**
```powershell
# Скопируйте папку data на сервер
scp -r data/ root@123.45.67.89:/home/officials/officials-tracker/
```

**На сервере:**
```bash
chown -R officials:officials /home/officials/officials-tracker/data/
```

Готово! Все ваши данные теперь на сервере.

### Вариант B: Миграция на PostgreSQL (для больших объёмов)

Если хотите мигрировать на PostgreSQL вместо JSON файлов:

**Создам отдельный скрипт миграции в следующем файле!**

---

## 👥 Как работают пользователи

### Streamlit Cloud или VPS:

**1. Вы (администратор):**
- Управляете сервером
- Обновляете данные из CSV
- Добавляете новые позиции

**2. Коллеги:**
- Открывают URL в браузере
- Добавляют упоминания
- Просматривают данные

**3. Все одновременно:**
- ✅ Можно работать вместе
- ✅ Изменения видны сразу
- ✅ Нет конфликтов

---

## 🔐 Безопасность

### Базовая защита:

**1. Добавьте авторизацию в Streamlit:**

Создайте файл `.streamlit/config.toml`:
```toml
[server]
enableCORS = false
enableXsrfProtection = true

[browser]
gatherUsageStats = false
```

**2. Используйте пароль (простой вариант):**

В `app.py` добавьте в начало:
```python
import streamlit as st

def check_password():
    """Returns `True` if user had correct password."""
    def password_entered():
        if st.session_state["password"] == "ваш_пароль_здесь":
            st.session_state["password_correct"] = True
            del st.session_state["password"]
        else:
            st.session_state["password_correct"] = False

    if "password_correct" not in st.session_state:
        st.text_input("Пароль", type="password", on_change=password_entered, key="password")
        return False
    elif not st.session_state["password_correct"]:
        st.text_input("Пароль", type="password", on_change=password_entered, key="password")
        st.error("😕 Неверный пароль")
        return False
    else:
        return True

if not check_password():
    st.stop()
```

**3. Используйте HTTPS** (обязательно для VPS)

---

## 📊 Сравнение Вариантов

| Фактор | Google Drive | Streamlit Cloud | VPS Сервер | Корп. Сервер |
|--------|-------------|-----------------|------------|--------------|
| Цена | $0 | $0-20/мес | $5-20/мес | Высокая |
| Настройка | 10 мин | 10 мин | 1-2 часа | Дни |
| Одновременный доступ | ❌ | ✅ | ✅ | ✅ |
| Скорость | Медленно | Быстро | Быстро | Очень быстро |
| Безопасность | Средняя | Средняя | Высокая | Максимальная |
| Контроль | Низкий | Средний | Полный | Полный |
| Техподдержка | Нет | Есть | Нет | Своя |

---

## 🎯 Рекомендации

### Для малых команд (2-5 человек):
➡️ **Streamlit Cloud** (бесплатно, просто)

### Для средних команд (5-20 человек):
➡️ **VPS Сервер** (DigitalOcean, $6/мес)

### Для крупных компаний:
➡️ **Корпоративный сервер** (максимальная безопасность)

---

## ✅ Следующие шаги

**Выберите вариант:**

1. **Streamlit Cloud** → Читайте "Вариант 1" выше
2. **VPS Сервер** → Читайте "Вариант 2" выше
3. **Нужна помощь?** → Напишите какой вариант выбрали, помогу настроить

---

## 📞 Поддержка

**Частые вопросы:**

**Q: Можно ли перейти с Google Drive на сервер постепенно?**
A: Да! Просто скопируйте папку `data/` на сервер.

**Q: Что если сервер упадёт?**
A: Systemd автоматически перезапустит. Плюс делайте бэкапы.

**Q: Сколько пользователей может работать одновременно?**
A: На VPS ($6/мес) - до 10-15 пользователей комфортно.

**Q: Можно ли вернуться на Google Drive?**
A: Да, просто скачайте папку `data/` с сервера.

---

**Готовы к серверному деплою?** 🚀

Скажите какой вариант выбираете, и я дам детальные инструкции!
