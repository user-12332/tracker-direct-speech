# Officials Tracker 🏛️

Система для отслеживания упоминаний государственных чиновников в СМИ.

## Возможности

- ✅ Управление списком позиций (должностей)
- ✅ Управление персонами (чиновниками) и их историей позиций
- ✅ Добавление упоминаний в медиа для каждой персоны
- ✅ Поиск и фильтрация данных
- ✅ Поддержка совместной работы через Google Drive
- ✅ File locking для предотвращения конфликтов

## Структура данных

```
officials_tracker/
├── data/
│   ├── positions/
│   │   └── positions.json          # Список всех позиций
│   ├── persons/
│   │   └── persons.json            # Список всех персон
│   └── mentions/
│       ├── person_001/             # Упоминания для каждой персоны
│       │   ├── 20240115_kommersant_mention_001.json
│       │   └── 20240220_rbc_mention_002.json
│       └── person_002/
│           └── ...
├── src/
│   └── core/
│       ├── models.py               # Модели данных
│       └── storage.py              # Работа с файлами
├── scripts/
│   └── import_from_excel.py        # Импорт из Excel
├── app.py                          # Streamlit приложение
└── config.py                       # Конфигурация
```

## Установка (локально)

### 1. Установите Python 3.8+

Проверьте версию:
```bash
python --version
```

### 2. Установите зависимости

```bash
pip install streamlit pandas openpyxl filelock
```

### 3. Скачайте проект

Скачайте папку `officials_tracker` на свой компьютер.

### 4. Запустите приложение

```bash
cd officials_tracker
streamlit run app.py
```

Откроется браузер с интерфейсом на `http://localhost:8501`

## Установка (Google Drive для команды)

### Настройка для совместной работы

1. **Установите Google Drive Desktop** на всех компьютерах
   - Windows/Mac: [скачать здесь](https://www.google.com/drive/download/)

2. **Создайте общую папку в Google Drive**
   - Создайте папку `officials_tracker`
   - Расшарьте её всем членам команды с правами редактирования

3. **Скопируйте проект в Google Drive**
   ```
   Google Drive/
   └── officials_tracker/
       ├── data/
       ├── src/
       ├── app.py
       └── ...
   ```

4. **Измените config.py**
   
   Откройте `config.py` и измените:
   ```python
   # Было:
   BASE_PATH = Path(__file__).parent.resolve()
   
   # Стало (для Mac/Linux):
   BASE_PATH = Path.home() / 'Google Drive' / 'officials_tracker'
   
   # Или для Windows:
   BASE_PATH = Path('G:/My Drive/officials_tracker')
   ```

5. **Запуск на каждом компьютере**
   ```bash
   cd ~/Google\ Drive/officials_tracker
   streamlit run app.py
   ```

### Как это работает

- Каждый работник запускает Streamlit **локально** на своем компьютере
- Все изменения сохраняются в Google Drive
- Google Drive автоматически синхронизирует файлы между компьютерами
- File locking предотвращает конфликты при одновременном редактировании

## Использование

### 1. Dashboard (Главная страница)

Показывает:
- Общую статистику (позиции, персоны, упоминания)
- Последние добавленные упоминания

### 2. Добавить упоминание

1. Выберите персону из списка
2. Заполните форму:
   - **Дата** - дата публикации
   - **Источник** - название СМИ (например: "Коммерсантъ")
   - **URL** - ссылка на статью
   - **Заголовок** - заголовок статьи
   - **Текст** - текст упоминания (основной контент)
   - **Теги** - ключевые слова через запятую
3. Нажмите "Сохранить"

**Обязательные поля:** Источник и Текст

### 3. Персоны

#### Вкладка "Список"
- Просмотр всех персон
- Поиск по имени
- Для каждой персоны видна история позиций и количество упоминаний

#### Вкладка "Добавить персону"
1. Введите ФИО
2. (Опционально) Назначьте первую позицию
3. Укажите дату начала работы
4. Отметьте, текущая ли это позиция

#### Вкладка "Изменить позицию"
1. Выберите персону
2. Выберите действие:
   - **Добавить новую позицию** - назначить на новую должность
   - **Закрыть текущую позицию** - указать дату окончания

### 4. Позиции

#### Вкладка "Список"
- Просмотр всех позиций, сгруппированных по ведомствам
- Поиск по названию
- Показывает текущих держателей позиции

#### Вкладка "Добавить позицию"
1. Название позиции (например: "Министр здравоохранения")
2. Ведомство (например: "Министерство здравоохранения РФ")
3. (Опционально) Департамент
4. Уровень: federal/regional/municipal

### 5. Все упоминания

- Просмотр всех упоминаний
- Фильтрация по персоне
- Фильтрация по источнику
- Сортировка по дате (новые первые)

## Импорт данных из Excel

Если у вас уже есть данные в Excel:

```bash
python scripts/import_from_excel.py
```

Формат Excel файла:
- Колонка A: Ведомство
- Колонка B: Департамент (опционально)
- Колонка C: Должность
- Колонка D: Имя персоны
- Колонка E: Дата начала
- Колонка F: Дата окончания (пусто если текущая)

## Советы по работе

### Ежедневный workflow

1. **Утро**: Откройте приложение
   ```bash
   streamlit run app.py
   ```

2. **Мониторинг новостей**: Для каждого чиновника проверяйте новости

3. **Добавление упоминаний**: 
   - Перейдите в "Добавить упоминание"
   - Выберите персону
   - Скопируйте текст из статьи
   - Заполните форму
   - Сохраните

4. **Обновление позиций**: Если кто-то сменил должность:
   - Перейдите в "Персоны" → "Изменить позицию"
   - Закройте старую позицию
   - Добавьте новую позицию

### Предотвращение конфликтов

При работе в команде через Google Drive:

- ✅ **Хорошо**: Каждый работник добавляет упоминания для разных персон
- ⚠️ **Осторожно**: Два работника редактируют одну персону одновременно
- ✅ **Решение**: File locking автоматически синхронизирует изменения

### Резервное копирование

Периодически делайте копию папки `data/`:
```bash
cp -r data/ backup_2024-12-01/
```

Или настройте автоматическое резервное копирование в Google Drive.

## Решение проблем

### Приложение не запускается

```bash
# Проверьте установку Streamlit
streamlit --version

# Переустановите зависимости
pip install --upgrade streamlit pandas openpyxl filelock
```

### "Module not found" ошибка

```bash
# Убедитесь, что вы в правильной директории
cd officials_tracker
python -c "import sys; print(sys.path)"
```

### Конфликты файлов в Google Drive

Если Google Drive создал файлы с суффиксом `(1)`:
1. Остановите приложение на всех компьютерах
2. Удалите дубликаты (файлы с `(1)`, `(2)` и т.д.)
3. Дождитесь синхронизации
4. Запустите приложение снова

### Медленная работа

Если данных стало много (>5000 упоминаний):
- Рассмотрите переход на PostgreSQL (Supabase)
- Архивируйте старые данные

## Расширение функциональности

### Добавление автоматического парсинга

Позже можно добавить:
- Автоматический парсинг новостных сайтов
- Telegram бот для быстрого добавления
- API для интеграций
- Экспорт в Excel/PDF

### Миграция на базу данных

Когда данных станет очень много, можно перейти на PostgreSQL:
```python
# В config.py
STORAGE_TYPE = 'postgres'
DATABASE_URL = 'postgresql://...'
```

## Контакты и поддержка

При возникновении проблем:
1. Проверьте раздел "Решение проблем" выше
2. Посмотрите логи в терминале
3. Создайте issue с описанием проблемы

## Лицензия

MIT License - используйте свободно для любых целей.
