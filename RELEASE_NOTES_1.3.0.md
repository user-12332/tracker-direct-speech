# 🎉 Version 1.3.0 - Release Notes

**Дата:** 2024-12-01

## ✨ Основные изменения

### 1. "Руководство" как отдел

**Что изменилось:**
- Все позиции теперь принадлежат отделу/департаменту
- Позиции без департамента (министры, заместители) теперь в отделе **"Руководство"**
- "Руководство" отображается как первый отдел в каждом ведомстве

**Было:**
```
Ведомство
├── Руководство (не отдел, а категория)
│   ├── Министр
│   └── Заместитель
└── Отделы и департаменты
    ├── Департамент А
    └── Департамент Б
```

**Стало:**
```
Ведомство
├── Руководство (отдел)
│   ├── Министр
│   └── Заместитель
├── Департамент А
└── Департамент Б
```

**Преимущества:**
- Единая структура: всё - это отделы
- Проще навигация: один уровень вложенности
- "Руководство" всегда показывается первым

### 2. Убраны эмодзи

**Что убрано:**
- ✅ ❌ 🟢 ⚪ 👤 ➕ 🔒 и другие эмодзи
- Заменены на текст: `[Активна]`, `[Неактивна]`, кнопки с текстовыми метками

**Причины:**
- Чище и профессиональнее выглядит
- Лучше читается
- Не зависит от шрифтов системы

**Примеры замен:**
- `🟢` → `[Активна]`
- `⚪` → `[Неактивна]`
- `👤` (кнопка) → `Назначить`
- `🔒` (кнопка) → `Деактивировать`
- `➕ Создать` → `Создать`

---

## 🔄 Как обновить данные

### Важно! После обновления нужно переимпортировать данные

**Старые данные:** позиции без subdepartment (subdepartment=null)
**Новые данные:** все позиции с subdepartment ("Руководство" или название отдела)

### Шаг 1: Обновите код

Замените файлы:
- `app.py` - новый интерфейс
- `scripts/import_from_csv.py` - обновленный импорт
- `config.py` - без эмодзи

### Шаг 2: Переимпортируйте данные

```powershell
# Windows
python scripts/import_from_csv.py list_officials.csv

# Mac/Linux
python3 scripts/import_from_csv.py list_officials.csv
```

Вы увидите:
```
✅ Import complete!
   Positions: 386
   Persons: 628

Sample positions (first 3):
  pos_001: Министр...
      Министерство... → Руководство
```

**Важно:** Видите `→ Руководство`? Значит всё правильно!

### Шаг 3: Перезапустите приложение

```powershell
# Остановите: Ctrl+C
streamlit run app.py

# В браузере нажмите 'C' для очистки кэша
```

---

## 📊 Статистика после обновления

```
Всего позиций: 386

Распределение:
├── В отделе "Руководство": 138
└── В других отделах: 248

Пример структуры:
Министерство экономического развития РФ (50 позиций)
├── Руководство (10 позиций)
│   ├── Министр
│   ├── Первый заместитель
│   └── ...
├── Департамент стратегического развития (3 позиции)
├── Правовой департамент (2 позиции)
└── ...
```

---

## 🎨 Визуальные изменения

### Навигация (без эмодзи):

**Было:**
```
📊 Dashboard
🏢 Организационная структура
➕ Добавить упоминание
👥 Персоны
```

**Стало:**
```
Dashboard
Организационная структура
Добавить упоминание
Персоны
```

### Отображение позиций:

**Было:**
```
▶ Министерство

  Руководство:
  🟢 Министр
     - 👤 Иванов И.И.
     [👤] [🔒]
  
  ---
  
  Департаменты:
  → Департамент А
     🟢 Директор
```

**Стало:**
```
▶ Министерство

  ▶ Руководство
     [Активна] Министр
        - Иванов И.И.
        [Назначить] [Деактивировать]
  
  ▶ Департамент А
     [Активна] Директор
```

---

## 🛠️ Технические изменения

### В models.py:

Модель Position теперь **всегда** имеет subdepartment:

```python
@dataclass
class Position:
    id: str
    title: str
    department: str
    subdepartment: str  # Больше не Optional! Всегда есть значение
    ...
```

### В import_from_csv.py:

```python
# Раньше:
if pd.notna(row['subdepartment']):
    current_subdepartment = row['subdepartment']
# subdepartment может быть None

# Теперь:
if pd.notna(row['subdepartment']) and row['subdepartment'].strip():
    current_subdepartment = row['subdepartment'].strip()
else:
    current_subdepartment = "Руководство"  # Всегда есть значение
```

### В app.py:

```python
# Раньше:
top_level = [p for p in positions if not p.subdepartment]
with_subdept = [p for p in positions if p.subdepartment]
# Разделение на две категории

# Теперь:
by_subdept = defaultdict(list)
for pos in positions:
    by_subdept[pos.subdepartment].append(pos)
# Все в отделах, "Руководство" показывается первым
```

---

## ⚠️ Breaking Changes

### 1. Структура данных positions.json

**Старый формат:**
```json
{
  "id": "pos_001",
  "title": "Министр",
  "subdepartment": null
}
```

**Новый формат:**
```json
{
  "id": "pos_001",
  "title": "Министр",
  "subdepartment": "Руководство"
}
```

### 2. API методы

Если вы использовали `get_persons_by_position()` с фильтром по subdepartment=None, теперь используйте:
```python
# Раньше:
top_positions = [p for p in positions if p.subdepartment is None]

# Теперь:
leadership_positions = [p for p in positions if p.subdepartment == "Руководство"]
```

---

## 🔄 Миграция старых данных

Если у вас уже были данные и упоминания:

### Вариант 1: Переимпорт (рекомендуется)

```powershell
# 1. Сделайте бэкап упоминаний
xcopy /E /I data\mentions data\mentions_backup

# 2. Переимпортируйте
python scripts/import_from_csv.py list_officials.csv

# 3. Упоминания останутся на месте (они не затрагиваются при импорте)
```

### Вариант 2: Ручная миграция (если не хотите переимпортировать)

```python
# migrate_to_rukovodstvo.py
import json

with open('data/positions/positions.json', 'r', encoding='utf-8') as f:
    data = json.load(f)

for pos in data['positions']:
    if pos.get('subdepartment') is None or pos['subdepartment'] == '':
        pos['subdepartment'] = 'Руководство'

with open('data/positions/positions.json', 'w', encoding='utf-8') as f:
    json.dump(data, f, ensure_ascii=False, indent=2)

print("✅ Migrated positions to 'Руководство'")
```

---

## ✅ Чек-лист обновления

- [ ] Скачали новые файлы (app.py, import_from_csv.py, config.py)
- [ ] Сделали бэкап папки data/ (опционально)
- [ ] Переимпортировали данные из CSV
- [ ] Проверили: `python verify_data.py` показывает 386 позиций
- [ ] Перезапустили приложение
- [ ] Нажали 'C' в браузере для очистки кэша
- [ ] Проверили: "Руководство" отображается как первый отдел
- [ ] Проверили: эмодзи убраны

---

## 🎯 Итог

### Что улучшилось:

✅ **Единая структура:** всё - это отделы, нет исключений
✅ **Чище интерфейс:** без эмодзи, более профессионально
✅ **Проще навигация:** "Руководство" всегда на первом месте
✅ **Проще код:** меньше условий, меньше разделения на категории

### Обратная совместимость:

⚠️ **Нужен переимпорт данных** - старые данные с null subdepartment не будут корректно отображаться
✅ **Упоминания сохраняются** - mentions/ не затрагиваются
✅ **История персон сохраняется** - persons.json обновляется автоматически при импорте

---

**Версия:** 1.3.0
**Дата:** 2024-12-01
**Тип обновления:** Major (breaking changes)
