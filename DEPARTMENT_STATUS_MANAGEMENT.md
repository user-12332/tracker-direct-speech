# 🏢 Department & Subdepartment Status Management

**Version:** 1.4.0  
**Date:** 2024-12-01

## ✨ Новая функциональность

Теперь можно **активировать** и **деактивировать** ведомства и отделы!

---

## 🎯 Зачем это нужно

### Проблема:
Ведомства и отделы иногда ликвидируются, реорганизуются, или временно приостанавливают работу.

**Раньше:** Позиции можно было деактивировать, но ведомства/отделы оставались видимыми.

**Теперь:** Можно деактивировать целое ведомство или отдел одной кнопкой!

---

## 📋 Что изменилось

### 1. Новые файлы данных

При импорте CSV теперь создаются два новых файла:

```
data/
├── departments.json       ← НОВОЕ! (49 ведомств)
├── subdepartments.json    ← НОВОЕ! (292 отдела)
├── positions.json
└── persons.json
```

**departments.json:**
```json
{
  "departments": [
    {
      "id": "dept_001",
      "name": "Министерство экономического развития РФ",
      "level": "federal",
      "is_active": true,
      "created_at": "2024-12-01T17:00:00",
      "deactivated_at": null
    }
  ]
}
```

**subdepartments.json:**
```json
{
  "subdepartments": [
    {
      "id": "subdept_001",
      "name": "Департамент инвестиционной политики",
      "department_name": "Министерство экономического развития РФ",
      "is_active": true,
      "created_at": "2024-12-01T17:00:00",
      "deactivated_at": null
    }
  ]
}
```

### 2. Новые кнопки в интерфейсе

**Для ведомств:**
```
▶ Министерство экономического развития РФ [Активно] (50 позиций)
                                                      [Деактивировать]
```

**Для отделов:**
```
  ▶ Департамент инвестиционной политики [Активно] (3 позиции)
                                                    [Деактивировать]
```

---

## 🎮 Как использовать

### Деактивация ведомства

**Сценарий:** Министерство ликвидировано или реорганизовано

**Шаги:**
1. Откройте страницу "Организационная структура"
2. Найдите ведомство
3. Нажмите **"Деактивировать"** справа от названия
4. Ведомство получит статус `[Неактивно]`

**Эффект:**
- Ведомство помечается как неактивное
- Все отделы внутри остаются видимыми (деактивируются отдельно при необходимости)
- Позиции остаются активными (деактивируются отдельно при необходимости)
- Запоминается дата деактивации

### Деактивация отдела

**Сценарий:** Департамент расформирован

**Шаги:**
1. Откройте ведомство
2. Найдите отдел
3. Нажмите **"Деактивировать"** справа от названия отдела
4. Отдел получит статус `[Неактивно]`

**Эффект:**
- Отдел помечается как неактивный
- Позиции внутри остаются активными (деактивируются отдельно)
- Запоминается дата деактивации

### Активация обратно

Любое деактивированное ведомство или отдел можно **активировать обратно**:

1. Найдите ведомство/отдел со статусом `[Неактивно]`
2. Нажмите **"Активировать"**
3. Статус вернётся к `[Активно]`
4. Дата деактивации очищается

---

## 📊 Отображение

### Активное ведомство:
```
▶ Министерство экономического развития РФ [Активно] (50 позиций)
  [Деактивировать]

  ▶ Руководство [Активно] (10 позиций)
     [Деактивировать]
     
     [Активна] Министр
        - Решетников М.Г.
        [Назначить] [Деактивировать]
```

### Неактивное ведомство:
```
▶ Министерство связи и массовых коммуникаций [Неактивно] (20 позиций)
  [Активировать]
  
  (отделы и позиции внутри всё ещё видны, но ведомство помечено)
```

### Неактивный отдел:
```
▶ Министерство экономического развития РФ [Активно] (50 позиций)

  ▶ Департамент X [Неактивно] (5 позиций)
     [Активировать]
     
     (позиции внутри всё ещё видны)
```

---

## 🔍 Логика работы

### Каскадность

**Деактивация НЕ каскадная:**
- Деактивация ведомства → отделы остаются активными
- Деактивация отдела → позиции остаются активными

**Почему:**
Это даёт гибкость. Вы можете:
1. Деактивировать только ведомство (пометить, что оно расформировано)
2. Оставить отделы активными (если они переданы другому ведомству)
3. Деактивировать позиции индивидуально (когда люди ушли)

### Рекомендуемый порядок при ликвидации:

```
1. Деактивировать все позиции в ведомстве
   ↓
2. Деактивировать все отделы
   ↓
3. Деактивировать ведомство
```

Или просто:
```
Деактивировать ведомство → оставить как есть (пометка, что расформировано)
```

---

## 🛠️ Технические детали

### Новые модели данных

**Department:**
```python
@dataclass
class Department:
    id: str
    name: str
    level: str = 'federal'
    is_active: bool = True
    created_at: str
    deactivated_at: Optional[str] = None
```

**Subdepartment:**
```python
@dataclass
class Subdepartment:
    id: str
    name: str
    department_name: str  # Parent department
    is_active: bool = True
    created_at: str
    deactivated_at: Optional[str] = None
```

### Новые методы StorageManager

```python
# Departments
storage.load_departments() -> List[Department]
storage.save_departments(departments: List[Department])
storage.get_department(name: str) -> Department
storage.update_department(department: Department)
storage.get_or_create_department(name: str, level: str) -> Department

# Subdepartments
storage.load_subdepartments() -> List[Subdepartment]
storage.save_subdepartments(subdepartments: List[Subdepartment])
storage.get_subdepartment(name: str, department_name: str) -> Subdepartment
storage.update_subdepartment(subdepartment: Subdepartment)
storage.get_or_create_subdepartment(name: str, department_name: str) -> Subdepartment
```

### Автоматическое создание при импорте

При запуске `import_from_csv.py`:

1. **Сканирует все позиции** → извлекает уникальные ведомства
2. **Сканирует все позиции** → извлекает уникальные отделы
3. **Создаёт departments.json** → все ведомства активны по умолчанию
4. **Создаёт subdepartments.json** → все отделы активны по умолчанию

**Результат импорта:**
```
✅ Import complete!
   Departments: 49       ← НОВОЕ!
   Subdepartments: 292   ← НОВОЕ!
   Positions: 386
   Persons: 628
```

---

## 📈 Статистика

После импорта ваших данных:

```
Ведомств: 49
  - Активных: 49 (все по умолчанию)
  - Неактивных: 0

Отделов: 292
  - Активных: 292 (все по умолчанию)
  - Неактивных: 0

Позиций: 386
  - Активных: 386
  - Неактивных: 0
```

**После деактивации:**

Пример - деактивировали Министерство связи:
```
Ведомств: 49
  - Активных: 48
  - Неактивных: 1

Отделов: 292
  - Активных: 292 (не затронуты)
  - Неактивных: 0
```

---

## 🔄 Обновление с предыдущей версии

### Важно!

Новые файлы (`departments.json`, `subdepartments.json`) создаются **только при импорте**.

### Шаг 1: Переимпортируйте данные

```powershell
python scripts/import_from_csv.py list_officials.csv
```

### Шаг 2: Проверьте созданные файлы

```powershell
dir data\departments.json
dir data\subdepartments.json
```

Оба файла должны существовать.

### Шаг 3: Перезапустите приложение

```powershell
streamlit run app.py
```

---

## 🎯 Примеры использования

### Пример 1: Реорганизация ведомства

**Ситуация:** Министерство связи объединили с Министерством цифрового развития

**Действия:**
1. Деактивировать "Министерство связи"
2. Создать новое ведомство "Министерство цифрового развития и связи"
3. Перенести сотрудников (создать новые позиции → назначить персон)
4. Старые позиции в Минсвязи можно оставить деактивированными (история)

### Пример 2: Расформирование отдела

**Ситуация:** Департамент инвестиционной политики расформирован

**Действия:**
1. Деактивировать все позиции в департаменте
2. Деактивировать департамент
3. Ведомство остаётся активным

### Пример 3: Временная приостановка

**Ситуация:** Ведомство временно не работает (реорганизация)

**Действия:**
1. Деактивировать ведомство
2. Когда работа возобновится → активировать обратно
3. История сохранена (дата деактивации записана)

---

## ✅ Преимущества

1. **Полная история:** Видно когда ведомство/отдел были деактивированы
2. **Гибкость:** Можно деактивировать на разных уровнях (ведомство/отдел/позиция)
3. **Обратимость:** Всегда можно активировать обратно
4. **Чистота интерфейса:** Неактивные помечены, но не удалены

---

## 🚀 Что дальше

В будущих версиях можно добавить:

- **Фильтр "Показать только активные"** - скрывать неактивные ведомства/отделы
- **История изменений** - кто и когда деактивировал
- **Каскадная деактивация** - опция деактивировать всё внутри одной кнопкой
- **Причина деактивации** - поле для заметок (ликвидация, реорганизация, и т.д.)
- **Статистика** - сколько ведомств/отделов деактивировано

---

**Версия:** 1.4.0  
**Тип обновления:** Feature  
**Breaking changes:** Нет (требуется переимпорт для создания новых файлов)
