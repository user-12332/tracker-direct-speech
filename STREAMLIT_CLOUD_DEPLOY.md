# 🚀 Streamlit Cloud - Пошаговая Инструкция

**Деплой вашего приложения за 15 минут**

---

## ✅ Что вы получите

**После деплоя:**
- URL типа: `https://oskol-officials-tracker.streamlit.app`
- Коллеги открывают ссылку в браузере
- Все работают одновременно
- Изменения видны сразу
- Никаких установок не требуется

---

## 📋 Что нужно

- ✅ GitHub аккаунт (бесплатно)
- ✅ 15 минут времени
- ✅ Этот проект

---

## 🎯 Шаг 1: Подготовка проекта (уже сделано!)

### Файлы для Streamlit Cloud уже созданы:

```
.streamlit/
├── config.toml         ✅ Создан
└── secrets.toml        ✅ Создан (шаблон)

.gitignore             ✅ Создан
packages.txt           ✅ Создан
requirements.txt       ✅ Есть
```

**Вы готовы к деплою!**

---

## 🐙 Шаг 2: GitHub (5 минут)

### 2.1. Создайте GitHub аккаунт (если нет)

1. Откройте: https://github.com/signup
2. Email: ваш email
3. Пароль: придумайте надёжный
4. Username: выберите (например: `oskol` или `oskol-officials`)
5. Подтвердите email

### 2.2. Создайте репозиторий

1. Нажмите **"+"** → **"New repository"**
2. Repository name: `officials-tracker`
3. Description: `Officials Tracker Application`
4. **Private** (если данные конфиденциальны) или Public
5. Нажмите **"Create repository"**

### 2.3. Загрузите проект

**Windows (PowerShell):**

```powershell
# Перейдите в папку проекта
cd C:\Users\oskol\Documents\GitHub\officials_tracker

# Инициализация Git (если ещё не сделано)
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit - Officials Tracker v1.7.4"

# Подключение к GitHub (замените YOUR_USERNAME на ваш username)
git remote add origin https://github.com/YOUR_USERNAME/officials-tracker.git

# Отправка на GitHub
git branch -M main
git push -u origin main
```

**Mac/Linux:**

```bash
cd ~/Documents/officials_tracker

git init
git add .
git commit -m "Initial commit - Officials Tracker v1.7.4"
git remote add origin https://github.com/YOUR_USERNAME/officials-tracker.git
git branch -M main
git push -u origin main
```

**При первой загрузке GitHub попросит авторизацию:**
- Username: ваш GitHub username
- Password: используйте **Personal Access Token** (не пароль!)

**Как создать Token:**
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Note: "officials-tracker"
4. Expiration: 90 days (или No expiration)
5. Выберите: `repo` (полный доступ к репозиториям)
6. Generate token
7. **СКОПИРУЙТЕ токен** (он больше не покажется!)
8. Используйте его вместо пароля при `git push`

---

## ☁️ Шаг 3: Streamlit Cloud (5 минут)

### 3.1. Зайдите на Streamlit Cloud

1. Откройте: https://share.streamlit.io/
2. Нажмите **"Sign in"**
3. Выберите **"Continue with GitHub"**
4. Авторизуйтесь через GitHub
5. Разрешите доступ Streamlit к GitHub

### 3.2. Deploy New App

1. Нажмите **"New app"** (большая кнопка)
2. **Repository:** выберите `YOUR_USERNAME/officials-tracker`
3. **Branch:** `main`
4. **Main file path:** `app.py`
5. **App URL (optional):** можете изменить (например: `officials-tracker-company`)

### 3.3. Advanced settings (опционально)

**Python version:** 3.9 (по умолчанию - ок)

**Secrets:** Оставьте пустым (пока не нужны)

### 3.4. Deploy!

1. Нажмите **"Deploy!"**
2. Подождите 2-5 минут
3. Увидите логи установки пакетов
4. Когда появится **"Your app is live!"** - готово!

---

## 🎉 Шаг 4: Получите URL

### Ваше приложение доступно по адресу:

```
https://YOUR_USERNAME-officials-tracker.streamlit.app
```

Или если изменили URL:
```
https://YOUR_CUSTOM_NAME.streamlit.app
```

**Откройте и проверьте что работает!**

---

## 👥 Шаг 5: Отправьте коллегам

### Напишите коллегам:

```
Привет!

Приложение Officials Tracker теперь доступно онлайн:

🔗 https://ваш-url.streamlit.app

Просто открой эту ссылку в браузере.

Что можешь делать:
✅ Добавлять упоминания чиновников
✅ Просматривать всех чиновников
✅ Искать по фильтрам

Мы можем работать одновременно!

Если есть вопросы - пиши.
```

**Готово! Они работают!** 🎉

---

## 🔄 Как Обновлять Данные

### Когда нужно импортировать новый CSV:

**На вашем компьютере:**

```powershell
cd C:\Users\oskol\Documents\GitHub\officials_tracker

# Импортируйте новые данные
python scripts/import_from_csv.py list_officials.csv

# Загрузите на GitHub
git add data/
git commit -m "Updated data from CSV - $(Get-Date -Format 'yyyy-MM-dd')"
git push
```

**Streamlit Cloud:**
- Автоматически подхватит изменения через 2-3 минуты
- Обновит приложение
- Коллеги увидят новые данные

---

## 📊 Мониторинг Приложения

### Streamlit Cloud Dashboard:

1. Зайдите: https://share.streamlit.io/
2. Увидите ваше приложение
3. **Status:** Running / Sleeping / Stopped
4. **Logs:** Смотрите логи в реальном времени
5. **Settings:** Изменяйте настройки

### Полезные метрики:

- **Viewers:** Сколько человек сейчас смотрят
- **Memory usage:** Использование RAM
- **CPU usage:** Нагрузка на процессор

---

## 🔐 Добавление Пароля (опционально)

### Если хотите защитить паролем:

**Шаг 1: Добавьте код в app.py**

В самое начало файла (после импортов):

```python
import streamlit as st

def check_password():
    """Returns `True` if the user had the correct password."""
    def password_entered():
        """Checks whether a password entered by the user is correct."""
        if st.session_state["password"] == st.secrets["password"]:
            st.session_state["password_correct"] = True
            del st.session_state["password"]
        else:
            st.session_state["password_correct"] = False

    if "password_correct" not in st.session_state:
        st.text_input(
            "Пароль", type="password", on_change=password_entered, key="password"
        )
        return False
    elif not st.session_state["password_correct"]:
        st.text_input(
            "Пароль", type="password", on_change=password_entered, key="password"
        )
        st.error("😕 Неверный пароль")
        return False
    else:
        return True

if not check_password():
    st.stop()

# Остальной код приложения...
```

**Шаг 2: Добавьте пароль в Secrets**

1. Streamlit Cloud → Ваше приложение → Settings (⚙️)
2. Secrets
3. Добавьте:
```toml
password = "ваш_надёжный_пароль"
```
4. Save

**Готово!** Теперь при входе нужен пароль.

---

## 🆘 Решение Проблем

### Проблема: "App is not running"

**Причина:** Приложение "засыпает" после бездействия (бесплатный план)

**Решение:** Просто откройте URL - проснётся за 30 секунд

---

### Проблема: "Module not found"

**Причина:** Забыли добавить зависимость в requirements.txt

**Решение:**
1. Добавьте недостающий пакет в `requirements.txt`
2. `git add requirements.txt`
3. `git commit -m "Added missing dependency"`
4. `git push`
5. Streamlit Cloud автоматически переустановит

---

### Проблема: "Out of memory"

**Причина:** Превышен лимит 1GB RAM (бесплатный план)

**Решение:**
- Вариант 1: Upgrade на Streamlit Cloud Pro ($20/мес)
- Вариант 2: Оптимизируйте приложение (удалите лишнее)
- Вариант 3: Перейдите на VPS

---

### Проблема: "Cannot connect to GitHub"

**Причина:** Неправильный token или нет доступа

**Решение:**
1. Проверьте GitHub token ещё активен
2. Создайте новый token если истёк
3. Используйте `git remote set-url origin https://TOKEN@github.com/USER/REPO.git`

---

## 📈 Следующие Шаги

### После успешного деплоя:

**✅ Работает:** Приложение доступно онлайн

**Можете:**
1. Добавить пароль (см. выше)
2. Настроить домен (Settings → Custom domain)
3. Включить аналитику (Settings → Analytics)
4. Upgrade на Pro если нужно больше ресурсов

**Обновления:**
- `git push` → автоматически обновляется
- Данные из CSV → импорт + push
- Изменения в коде → commit + push

---

## 💰 Стоимость

### Streamlit Cloud Free:
- ✅ 1 приложение бесплатно
- ✅ 1GB RAM
- ✅ Достаточно для 5-10 одновременных пользователей
- ✅ Хватит для 100,000+ упоминаний
- ⚠️ "Засыпает" после бездействия (просыпается за 30 сек)

### Streamlit Cloud Pro ($20/месяц):
- ✅ 3 приложения
- ✅ 2GB RAM
- ✅ Не засыпает
- ✅ Priority support
- ✅ Custom domains

**Для начала бесплатного хватит!**

---

## 🎯 Чеклист Деплоя

- [ ] GitHub аккаунт создан
- [ ] Репозиторий `officials-tracker` создан
- [ ] Проект загружен на GitHub (`git push`)
- [ ] Streamlit Cloud аккаунт создан
- [ ] Приложение задеплоено (Deploy!)
- [ ] Приложение открывается по URL
- [ ] URL отправлен коллегам
- [ ] Коллеги могут открыть и работать

**Все галочки? Поздравляю!** 🎉

---

## 📞 Нужна Помощь?

### Застряли на каком-то шаге?

**Напишите мне:**
- "Не могу создать GitHub аккаунт"
- "Git выдаёт ошибку"
- "Streamlit Cloud не деплоит"
- "Приложение не открывается"

**Я помогу решить любую проблему!**

---

## 🎊 Поздравляю!

**Вы сделали это!**

- ✅ Приложение в облаке
- ✅ Доступно по URL
- ✅ Коллеги работают одновременно
- ✅ Обновления автоматические
- ✅ Бесплатно!

**Теперь ваша команда может эффективно работать!** 🚀

---

**Version:** 1.8.0 - Streamlit Cloud Ready  
**Time to deploy:** 15 минут  
**Cost:** $0  
**Status:** 🚀 Production Ready
